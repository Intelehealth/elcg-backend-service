# configuration

Master data + feature flags + theme + dynamic form schemas.

**Status: stub.** Healthcheck is up.

Port: `3004`. Modules planned: master data CRUD, feature flags engine (Nepal gating for IHRDMP-627 Stage-3), theme, patient-registration schema, visit-section schema, vitals, LCG/partograph config, WebRTC settings.
